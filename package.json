{
  "name": "battleship-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "cd apps/server && npm run start",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "check": "turbo run check",
    "migrate": "echo 'No migrations to run'",
    "pre-deploy": "pnpm run lint && pnpm run check && pnpm run test && pnpm run build"
  },
  "devDependencies": {
    "turbo": "^2.9.9"
  }
}
